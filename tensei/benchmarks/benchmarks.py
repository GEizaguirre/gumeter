def run_benchmark(benchmark_name, backend):
    print(f"Running benchmark '{benchmark_name}' with backend '{backend}'...")


def run_all_benchmarks(backend):
    print(f"Running all benchmarks with backend '{backend}'...")
